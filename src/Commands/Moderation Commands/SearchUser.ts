import { User } from "discord.js";

/**
 * This function search user by params.
 *
 * @category Services
 * @param {User} user - String to search user.
 * @returns {String} user alias or name.
 */
export function searchUser(user: User) {}
